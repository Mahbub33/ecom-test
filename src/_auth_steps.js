/***
 * 1. create a new project in console.google.lcom
 * 2. npm install
 * 3. create firebase.init.js and import getAuth to export auth
 * 4. firebase settings > authentication > enable email and password auth /now auth method initialize through sideline authentication
 * 5
*/


/***
 * hostin steps
 * 1. npm install -g firebase-tools 
 * 2. firebase login
 * 3. firebase init
 * 4. firebase deploy
 */